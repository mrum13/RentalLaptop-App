<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mbeedev.rentop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon_fix"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon_fix_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rentop">
        <activity android:name=".SewaActivity2"></activity>
        <activity android:name=".SewaActivity" />
        <activity android:name=".DetailMerkActivity" />
        <activity android:name=".MerkActivity" />
        <activity android:name=".AlamatActivity" />
        <activity android:name=".UbahProfilActivity" />
        <activity android:name=".AkunActivity2" />
        <activity android:name=".DaftarActivity" />
        <activity android:name=".SplashActivity2" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>