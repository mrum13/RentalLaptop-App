<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AkunFragment"
    android:background="@color/bg">

    <include
        layout="@layout/toolbar_akun"
        android:id="@+id/toolbar_akun"/>

    <TextView
        android:id="@+id/text_akun"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/akun_text"
        android:textColor="@color/text"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/toolbar_akun"
        android:layout_marginTop="44dp"
        android:textAlignment="center"
        android:layout_marginHorizontal="50dp"
        android:paddingVertical="20dp"
        android:gravity="center_horizontal" />
    
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="89dp"
        android:layout_marginHorizontal="81dp"
        android:text="@string/btn_keluar_text"
        android:paddingVertical="13dp"
        android:id="@+id/btn_keluar"/>

</RelativeLayout>